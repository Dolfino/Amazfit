apiVersion: v1
kind: Service
metadata:
  name: amazfit-backend-service
  namespace: default
  labels:
    app: amazfit-backend
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: amazfit-backend
