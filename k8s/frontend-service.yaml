apiVersion: v1
kind: Service
metadata:
  name: amazfit-frontend-service
  namespace: default
  labels:
    app: amazfit-frontend
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: amazfit-frontend
